import App from '/Users/owner/Desktop/Node/lokiNewTest/src/app.js';

window.addEventListener('load', function() {
    console.log('Attempting to init app!');
    var app = new App({selector: 'loki-app', toplevel: true});
    window['app'] = app;
}); 